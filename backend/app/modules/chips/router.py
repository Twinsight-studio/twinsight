"""籌碼面資料（三大法人買賣超、融資融券） — API routes.

TODO: no business logic here yet. Wire actual endpoints once product spec
for the chips module is ready.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/chips", tags=["chips"])

# TODO: define chips endpoints (router.get / router.post ...).
