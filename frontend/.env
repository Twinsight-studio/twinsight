# Backend API base URL (Cloud Run Service URL in prod, localhost in dev).
# Public: no secrets belong in NEXT_PUBLIC_* — Fugle keys etc. stay backend-only.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
